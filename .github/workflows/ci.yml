name: My First CI

on: push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: check project
        run: |
          echo "Project check passed!"
          exit 1
